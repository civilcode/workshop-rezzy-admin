<h2>New comment</h2>

<%= render "form.html", changeset: @changeset,
                        action: reservation_comment_path(@conn, :create, @reservation) %>

<%= link "Back", to: reservation_path(@conn, :show, @reservation) %>
